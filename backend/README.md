# Dublin Bike-Sharing Backend

This folder contains the Flask backend, scrapers, and ML logic for the Dublin Bike-Sharing application.

## Development

- Use `uv sync` to install dependencies.
- Use `uv run python app.py` to start the server.
